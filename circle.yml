machine:
  python:
    version: 2.7

dependencies:
  cache_directories:
    - "/opt/spm8"

  pre:
    # Add MCRInstaller.bin dependencies first
    - sudo apt-get install -y libxp6 libxpm4 libxmu6 libxt6
    - source continuous_integration/setup_spm.sh

test:
  override:

general:
  artifacts:
